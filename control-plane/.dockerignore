# Rebuilt inside the image — never copied in.
node_modules
.next

# Local secrets/config: the container gets DATABASE_URL from its environment
# (docker-compose), never from a baked-in .env.
.env
.env.*

npm-debug.log*
